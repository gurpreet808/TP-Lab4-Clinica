import { FechaDiaMesPipe } from './fecha-dia-mes.pipe';

describe('FechaDiaMesPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaDiaMesPipe();
    expect(pipe).toBeTruthy();
  });
});
