import { ResaltarEstadoTurnoDirective } from './resaltar-estado-turno.directive';

describe('ResaltarEstadoTurnoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarEstadoTurnoDirective();
    expect(directive).toBeTruthy();
  });
});
